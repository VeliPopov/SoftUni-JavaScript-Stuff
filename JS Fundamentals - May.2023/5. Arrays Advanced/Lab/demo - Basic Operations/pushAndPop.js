function demo() {

    let array = [1, 2, 3, 4]
    //array [4] = 5; - вариантът на долиня ред е по-правилния
    array.push(5)

    //вади елемент от края на масива и го връща като резултат от изпълнението на функцията
    //array.pop()
    let result = array.pop() // казва - махни един елемент от края на масива и ми го дай, като го запазиш в тази променлива
    console.log(result);
    console.log(array);
}
demo()