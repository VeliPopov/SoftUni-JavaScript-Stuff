function demo(num1, num2, name, age) {
    let result = num1 + num2;
    console.log(result);

    let textForPrint = `${name} - > ${age + result}`;
    console.log(textForPrint);
    
}
demo(18, 24, 'V', 37)



// A function is a named subprogram designed to perform a particular task
// Functions are executed when they are called. This is known as invoking a function
// Values can be passed into functions and used within the function
// Функцията се декларира с параметър и се извиква с аргумент


