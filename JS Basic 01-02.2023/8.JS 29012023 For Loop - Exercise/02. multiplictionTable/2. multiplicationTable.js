function multiplictionTable(input){
    let n = Number(input[0]);

    for (let i = 1; i <= 10; i++) {
        console.log(`${i} * ${n} = ${i * n}`);
    }
}
multiplictionTable(["5"]);


// по-кратък код - по-добро решение