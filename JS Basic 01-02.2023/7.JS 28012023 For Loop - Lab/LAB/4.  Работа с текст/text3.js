function characterSequence(input) {
    let text = input[0];

    // for (let i = 0; i < text.length; i++) { 
     //     console.log(text[i]);
    // }
    console.table(input)
    //дава стойността зад всеки индекс  - изкарва го в табличен вид

}
characterSequence(["i", "c", "e", "", "c", "r", "e", "a", "m"]);
//characterSequence(["ice cream"]);