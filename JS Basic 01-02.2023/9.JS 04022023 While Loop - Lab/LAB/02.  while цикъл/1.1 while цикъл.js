function solve(){
    let index = 0;
    let str = input[index];
    while (str !== "Stop") {
      console.log("Infinite loop");
      index++;
      str = input[index];
    }
//уславие за прекратяване на повторението
//правилния начин да се прави    
}
solve()