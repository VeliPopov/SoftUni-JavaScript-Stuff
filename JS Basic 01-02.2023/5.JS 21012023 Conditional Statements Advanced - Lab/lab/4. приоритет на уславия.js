//Чрез скоби ( ) можем да приоритизираме условия 
//еднакъв код, но скобите са на различни места и така изхода е различен
let a = 50;
let b = 200;
let c = 300;
 
if ((a >= 100 && b <= 200) || (c + b >= 300 && c <= 400)) {
  consоle.log("Yes"); // Yes
}

if (a >= 100 && (b <= 200 || c + b >= 300) && c <= 400) {
  console.log("Yes"); // No output
}
