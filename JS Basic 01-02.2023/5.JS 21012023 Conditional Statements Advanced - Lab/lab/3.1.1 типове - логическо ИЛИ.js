// Логическото ИЛИ || проверява дали е изпълнено поне едно измежду няколко условия
// Пример: проверка дали получената дума е:
// "Example" или "Demo"

// поне едната проверка в израза трябва да бъде ВЯРНА - true

function solve(input) {	
    let word = input[0];

    if (word == "Example" || word == "Demo");
} 
solve()

// вложени порверки
if (word == "Example"){ 
}
else if (word =="Demo"){
}

// логически оператор ||
if (word == "Example" ||  word =="Demo"){
}