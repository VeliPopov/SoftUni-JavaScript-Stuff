function solve() {
    let i = 0;
    while (i < 6) {
        i++; // инкерементацията е преди проверката, затова в проверката i не може да има стойност 0
        if (i % 2 == 0)
            console.log(i);
    }
    //ще изведе само четните числа   
}
solve();

function solve() {
    let i = 0;
    while (i < 6) {
        if (i % 2 == 0) {
            console.log(i);
        }
        i++; //тук ако е проверката резултата ще е 0, 2 и 4
    }
    //ще изведе само четните числа   
}
solve();