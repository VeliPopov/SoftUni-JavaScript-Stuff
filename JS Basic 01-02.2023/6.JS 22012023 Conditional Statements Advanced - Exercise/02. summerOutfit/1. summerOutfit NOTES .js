function summerOutfit(input) {
    let degrees = Number(input[0]);
    let partOfDay = input[1];


    //TODO: тази променлива се подава така като стринг (в кавички ' '), когато неизвестната стойност е дума. 
    //todo: Еквивалентно е на = 0 при неизвестно число.
    let outfit = '';
    let shoes = '';

    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }



}
summerOutfit();